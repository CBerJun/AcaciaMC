# Test companion of `module.aca`

#* Circular import test *#
# import .module

#* Cache test *#
# `module.aca` has imported `print`, so here it should use cache
import print

def test(x: int) -> bool:
    result := x >= 0

inline def get_inline_func():
    a: int = 0
    inline def inline_func():
        a += 1
        print.tell(print.format("a: %0", a))
    result -> inline_func
