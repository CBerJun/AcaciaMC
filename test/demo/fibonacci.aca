# Output the Fibonacci Sequence up to `MAX`
# 斐波那契数列计算
# 输出到MAX为止的斐波那契数列

import print

const MAX = 114514

a := 0
b := 1
while b <= MAX:
    swap(a, b)
    b += a
    print.tell(print.format("%0", a))
