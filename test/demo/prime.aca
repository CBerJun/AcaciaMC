# Output all the prime numbers less than `MAX`
# 输出所有小于MAX的质数

import print

const MAX = 50

def is_prime(x: int) -> bool:
    #**
     * Test if an integer is a prime number.
     * `x` has to be larger than 1.
     *#
    mod := 2
    res := True
    while mod <= x / 2 and res:
        res = (x % mod == 0)
        mod += 1
    result res

i := 2
while i <= MAX:
    if is_prime(i):
        print.tell(print.format("%0", i))
    i += 1 
