# Output all the prime numbers less than `MAX`
# 输出到MAX为止的所有质数

import print

MAX -> 50

def is_prime(x: int) -> bool:
    # Test if an integer is a prime number
    # `x` must >= 2
    result := True
    mod := 2
    while mod <= x / 2:
        if x % mod == 0:
            result = False
        mod += 1

i := 2
while i <= MAX:
    if is_prime(i):
        print.tell(print.format("%0", i))
    i += 1 
